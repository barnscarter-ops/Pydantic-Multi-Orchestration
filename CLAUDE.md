# HomeLab — Claude Code Notes

## Git Branches
- Main branch: `main`
- When working from a mobile session, always push to a branch named `mobile` (not auto-generated names like `claude/random-name-xyz`)
- The user will pull and merge `mobile` into `main` when back at their PC
